---
title: "Лекция 3. Исследования скважин и пластов"
author: "Ринат Хабибуллин"
format: 
    revealjs: 
        smaller: true
        #chalkboard: true
        embed-resources: true
---


## Уравнение фильтрации


$$ 
    \frac{\partial ^2 p }{\partial r^2} + \frac{1}{r} \frac{\partial p}{\partial r} = \frac{\varphi \mu c_t}{0.00036 k} \frac{\partial p}{\partial t} 
$$ {#eq-diff_eq_2}

-   $k$ - проницаемость, мД
-   $\mu$ - вязкость флюида, сП
-   $p$ - давление, атм
-   $r$ - расстояние, м
-   $\rho$ - плотность флюида, кг/м$^3$
-   $\varphi$ - пористость породы, доли единиц.
-   $c_t$ - общая сжимаемость породы и флюида, 1/атм
-   $t$ - время, час

## Уравнение фильтрации в безразмерных переменных
$$ 
\frac{\partial p_D}{ \partial t_D} = \frac{1}{r_D}\left[ \frac{ \partial{}}{ \partial{r_D} }\left( r_D \dfrac{\partial p_D}{ \partial r_D} \right) \right]  
$$ {#eq-equation_dimensionless}

* $r_D$ - безразмерное расстояние от центра скважины
* $t_D$ - безразмерное время
* $p_D$ - безразмерное давление

## Определения безразмерных переменных

$$ 
r_D = \frac{r}{r_w} 
$$

$$ 
t_D = \frac{0.00036 kt}{\varphi \mu c_t r_w^2}
$$

$$ 
p_D = \frac{kh}{ 18.42 q_s B \mu} \left( p_i - p_{wf} \right) 
$$

$$ 
q_D = \frac{q}{q_{ref}} 
$$


## Решение линейного стока

-   начальное условие.
$$
t_D < 0, p_D = 0 
$$ 

-   граничное условие на скважине 
$$ 
\lim_{r_D \to 0} {r_D \frac{\partial p_D}{\partial r_D}} = -1  
$$

-  граничное условие на контуре питания
$$ 
r_D = \infty, p_D = 0 
$$

Решение  
$$ 
p_D(r_D,t_D) = - \frac{1}{2} Ei \left(- \dfrac{ r_D^2}{4t_d} \right)
$$ {#eq-eq_solution_d}

где -Ei(-x) - интегральная показательная функция.


## Решение линейного стока в размерных переменных {.smaller}


$$
p\left(r,t\right)=p_i-\frac{18.42q_sB\mu}{kh}\left(-\frac{1}{2}Ei\left(-\frac{\varphi\mu c_tr^2}{0.00144kt}\right)\right) 
$$ {#eq-ln_solution}


-   $r_w$ - радиус скважины, м
-   $r$ - расстояние от центра скважины до точки в пласте, м
-   $q_s$ - дебит скважины на поверхности, в нормальных условиях м^3^/сут
-   $\phi$ - пористость, доли единиц
-   $\mu$ - вязкость нефти в пласте, сП
-   $B$ - объемный коэффициент нефти, м^3^/м^3^
-   $p_i$ - начальное давление в пласте, атм
-   $p$ - давление на расстоянии $r$, атм
-   $c_t$ - общая сжимаемость системы в пласте, 1/атм

## Логарифмическая аппроксимация решения линейного стока


Решение с учетом логарифмической аппроксимации в размерных переменных

$$
p\left(r,t\right)=p_i-\frac{9.21q_sB\mu}{kh}\left(ln{\frac{kt}{\varphi\mu c_tr^2}}-7.12\right)
$$ {#eq-ei_solution_full}

верно при 
$$
\frac{kt}{\varphi\mu c_tr^2}>70000 
$$ {#eq-eq_sol_condition}

## Влияние ствола скважины

![Изменение дебита при влиянии ствола скважины](wbs_1.png)

$$
C_s = - \frac{\Delta V}{\Delta P}
$$

Единицы измерения Cs [м3/атм]

## $C_s$ для фонтанирующей скважины
Для фонтанирующей скважины
Изменение объема жидкости в стволе скважины происходит за счет
сжимаемости жидкости
$$
\Delta V = - c V_w \Delta P
$$

$$
C_s = - \frac{\Delta V}{\Delta P}
$$

Для фонтанирующей скважины
$$
C_s = c V_w
$$

- $C_s$ - коэффициент влияния ствола скважины [м3/атм]
- $V_w$ - объем жидкости в стволе скважины [м3]
- $c$ - cжимаемость жидкости в стволе скважины [1/атм]

## $C_s$ для механизированной скважины скважины

::: {layout-ncol=2}
![Скважина с дин уровнем](wbs_2.png)

$$
C_s = - \frac{\Delta V}{\Delta P}
$$
$$
\Delta V = A_{cas} \Delta h
$$ 
$$
\Delta P = \rho g h
$$
:::
$$
C_s = \frac{A_{cas}}{\rho g}
$$

- $A_{cas}$ - Площадь поперечного сечения затрубного
пространства, м2
- $\Delta h$ - Изменение уровня жидкости в затрубном
пространстве, м

## Изменение давления во время влияния ствола скважины


![Изменение давления](wbs_3.png)

## Изменение дебита из пласта при послепритоке

$$
q_f = q + \frac{24 C_s}{B} \frac{dP}{dt}
$$

- $q_f$ - приток из пласта в поверхностных условиях м^3^/сут
- $q$ - поверхностный дебит, м^3^/сут
- $B$ - объемный коэффициент нефти, м^3^/м^3^
- $t$ - время, час
- $P$ - давление, атм
- $C_s$ - коэффициент влияния ствола скважины м^3^/атм

# Интерпретация ГДИС

## Исследование на падение давления


![Тест на падение давления](test_draw_down.png)

## Решение линейного стока


$$
p\left(r,t\right)=p_i-\frac{18.42q_sB\mu}{kh}\left(-\frac{1}{2}Ei\left(-\frac{\varphi\mu c_tr^2}{0.00144kt}\right) + S\right) 
$$ 

Логарифмическая аппроксимация

$$
p\left(r,t\right)=p_i-\frac{9.21q_sB\mu}{kh}\left(ln{\frac{kt}{\varphi\mu c_tr^2}}-7.12 + 2S\right)
$$ 

верно при 
$$
\frac{kt}{\varphi\mu c_tr^2}>70000 
$$ 

## MDH


![Тест на падение давления](mdh1.png)


## MDH

$$
p_{wf} = m_{ln} \ln t + p_{t=1}
$$

$$
p\left(r,t\right)=p_i-\frac{9.21q_sB\mu}{kh}\left(\ln t + \ln{\frac{k}{\varphi\mu c_tr^2}}-7.12 + 2S\right)
$$ 

Откуда


::: {layout-ncol=2}

$$
k = 9.21 \frac{q_s B \mu}{m_{ln} h}
$$

$$
S = \frac{1}{2} \left[ \frac{p_i - p_{t=1}}{m_{ln}} - \ln{\frac{k}{\phi \mu c_t r_w^2}} + 7.120 \right]
$$
:::

## КВД


![Тест на восстановление давления](test_build_up_1.png)


## КВД


![Тест на восстановление давления](test_build_up_2.png)


## КВД


![Тест на восстановление давления](test_build_up_3.png)


## КВД


![Тест на восстановление давления](test_build_up_2.png)

$$
p_{ws} = p_i - \frac{18.42 q B \mu}{kh} \left[ \frac{1}{2} \left( \ln \frac{k(t_p+\Delta t)}{\phi \mu c_t r_w^2} - \ln \frac{k \Delta t}{\phi \mu c_t r_w^2} \right) \right]
$$

$$
p_{ws} = p_i - \frac{9.21 q B \mu}{kh} \frac{t_p + \Delta t}{ \Delta t}
$$

## Метод Хорнера



![Тест на восстановление давления](test_build_up_4.png)

## Метод Хорнера - скин фактор

*Скин-фактор* определяется по разнице между давлениями замеренными:

- после 1 часа восстановления давления $\Delta t =1$

$$
p_i - p_ws(\Delta t =1) = 9.21 \frac{q B \mu}{kh} \ln (t_p+1)
$$

- и измеренного в момент закрытия $p_wf(t_p)$ = $p_ws(\Delta t =0)$

$$
p_i - p_wf(t_p) = 9.21 \frac{q B \mu}{kh} \left[ \ln t_p + \ln \frac{k}{\phi \mu c_t r_w^2 - 7.12 + 2 S} \right]
$$

Выражение для скин-фактора примет вид 

$$
S = \frac{1}{2} \left [ \frac{p_{ws}(\Delta t =1) - p_{wf}(t_p)}{m_{ln}} + \ln \frac{t_p + 1}{t_p} - \ln \frac{k}{\phi \mu c_t r_w^2} + 7.12 \right]
$$
